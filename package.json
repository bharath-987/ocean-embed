{
  "name": "ocean-embed",
  "version": "1.0.0",
  "description": "Satellite-derived ocean temperature depth profiles for the north Indian Ocean.",
  "scripts": {
    "dev": "serve . --listen 5500",
    "build": "npx esbuild src/index.tsx --bundle --outfile=dist/kyogre-app.js --loader:.tsx=tsx --loader:.ts=ts --format=iife --global-name=KyogreApp --external:react --external:react-dom --external:react-dom/client \"--banner:js=var require = function(m) { if (m==='react') return window.React; if (m==='react-dom/client'||m==='react-dom') return window.ReactDOM; throw new Error('Cannot find module ' + m); };\""
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "serve": "^14.2.3"
  }
}
